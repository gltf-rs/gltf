language: rust

rust:
    - stable
    - 1.15
    - beta
    - nightly

sudo: false

script:
    - cargo build --verbose --all-features
    - cargo test --verbose --all-features

os:
    - linux
    - osx

matrix:
    allow_failures:
        - rust: nightly
        - rust: beta
