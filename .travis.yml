language: rust

rust:
    - stable
    - beta
    - nightly

sudo: false

addons:
    apt:
        packages:
            - libgl1-mesa-dev

os:
    - linux
    - osx

matrix:
    allow_failures:
        - rust: nightly
        - rust: beta
